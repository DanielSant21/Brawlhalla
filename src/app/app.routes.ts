import { Routes } from '@angular/router';
import { PgPrincipalComponent } from './Pages/pg-principal/pg-principal.component';

export const routes: Routes = [
    {
        path: '', component: PgPrincipalComponent
    }
];
